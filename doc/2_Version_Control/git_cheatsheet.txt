git commit
    A commit in a git repository records a snapshot of all
     the (tracked) files in your directory. It's like a giant 
     copy and paste, but even better!
git branch
    list your branches. a * will appear next to the currently
     active branch
git checkout
    switch to another branch and check it out into your 
    working directory
git pull
    fetch and merge any commits from the tracking remote branch
git push
    Transmit local branch commits to the remote repository branch
git merge
    merge the specified branch’s history into the current one
git rebase
    apply any commits of current branch ahead of specified one
git fetch
    fetch down all the branches from that Git remote